---
title: display 속성이 pre 태그에 미치는 영향
description:
keywords: css
---

코드블록을 표현할 때 사용하는 pre를 `display: grid` 에서 사용할 때는 계속 부모 엘리먼트의 width를 넘어가는데, `display: flex` 로 바꾸니 의도한 대로 부모 엘리먼트의 width를 넘어가지 않았다. 그리드 레이아웃의 특수성이 있나보다.
