---
title: twoDigitsNumber
description: 한 자리의 number 타입 값을 두 자리의 numeral 문자열로 출력하는 함수
---

```ts title="utils/two-digits-number.ts"
export function twoDigitsNumber(digit: number): string {
  return digit < 10 ? `0${digit}` : String(digit)
}
```
